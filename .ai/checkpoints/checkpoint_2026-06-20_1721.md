# Checkpoint 4 — 401 Loop Fix Complete

**Date:** 2026-06-20 17:21 UTC-4
**Session:** v4 Full-Stack Agent Harness — Deep Diagnosis
**Status:** COMPLETE

---

## What Was Done

1. **Diagnostic Agent**: Root cause identified — FastAPI `HTTPException` wraps `detail` in `{"detail": ...}`, but `useAuthSession.ts` parsed `data.error.code`, causing all 401s to misclassify as `TRANSIENT_ERROR` → infinite retry loop.

2. **Code Agent**: Implemented fix in `frontend/src/auth/useAuthSession.ts`: explicit `status === 401` guard transitions to `SIGNED_OUT` before body parsing.

3. **Test Agent**: Completely rewrote `frontend/src/auth/__tests__/AuthProvider.test.tsx` to fix pre-existing infrastructure bugs (incomplete env mock, React `ref` reserved prop, missing fetch mock, synchronous assertions in async React 18). Added 401 → SIGNED_OUT regression test.

4. **Codebase Context Agent**: Updated `codebase-map.json` timestamp and `last_touched` entries.

5. **Documentation Agent**: Updated `drift-log.md` with root cause, fix, verification, and new drift detections.

6. **Diagnostic Agent**: Generated `.ai/diagnostics/2026-06-20_401-loop_report.md` with full findings, cross-references, and triage recommendations.

---

## Verification Results

| Check | Result |
|---|---|
| `AuthProvider.test.tsx` | ✅ 4/4 pass |
| `tsc --noEmit` | ✅ pass |
| Full vitest suite | ⚠️ 111/135 pass (24 pre-existing failures in curriculum/billing/auth tests, unrelated to this change) |

---

## Files Modified

| File | Change |
|---|---|
| `frontend/src/auth/useAuthSession.ts` | 401 → `SIGNED_OUT` guard in `handleError` |
| `frontend/src/auth/__tests__/AuthProvider.test.tsx` | Fixed test infrastructure + 401 regression test |
| `.ai/context/drift-log.md` | Added session entry for 401 loop diagnosis |
| `.ai/context/codebase-map.json` | Updated timestamp and `last_touched` |
| `.ai/diagnostics/2026-06-20_401-loop_report.md` | Fresh diagnostic report |

---

## Next Session Recommendations

- **High priority**: Align 401 envelope shape between backend and frontend (ADR 0024 §B5 contract violation)
- **Medium priority**: Add `useMemo` to `useCredentialSource()` for stable references
- **Medium priority**: Fix 24 pre-existing test failures in curriculum/billing/auth tests
