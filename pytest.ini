[pytest]
addopts = --cov=backend --cov-report=term-missing --cov-fail-under=25
testpaths = backend/tests
markers =
    integration: DB or external system tests
    slow: Tests that take >1s or require full app startup
